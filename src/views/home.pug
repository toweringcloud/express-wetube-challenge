extends layout
include mixins/video

block content
  center
    h2=pageTitle
  p.info ✨ Please check your favorite video(s).
  if videos && videos.length > 0
    div.grid-container
      each item in videos
        +video(item)
  else
    li Sorry, No Videos!